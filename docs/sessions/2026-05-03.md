# Session: 2026-05-03 (Sunday afternoon)

Operator: dwilliams + Claude Opus 4.6
Compute: Claude weekly limits just reset, full budget. No GPT for ~3 days.

## Starting state

Merged `overnight/20260430-231647` (Codex/gpt-5.5) at session start: 49 matched, 51 failed, 0 errors.

| Status | Functions | Bytes |
|--------|-----------|-------|
| Matched | 4,006 | 528,832 |
| Failed | 738 | 145,664 |
| Untried | 4,498 | 2,909,068 |

Recent run trend (all Codex):
- 20260430-231647: 49/100 = 49% hit, avg matched 152B, 3h
- 20260430-201648: 24/34 = 71% hit, avg matched 198B, 1.6h
- 20260430-110105: ~189 matched, high volume, avg 144B

Hit rate by size shows a hard cliff at 512B: ≤512B functions match at 78-93%, >512B functions match at 9.5% (2/21 tried).

## Assessment

The project is at an inflection point — "mid-game." The easy small functions are largely done and hit rates are declining. 70% of remaining bytes live in functions >512B that we basically can't match yet.

Three structural blockers identified:

1. **Read/cReadBlock prologue divergence** — the dominant single failure mode. 191 failed + 452 untried Read methods (~200KB). 166/194 failure notes cite "prologue" divergence with cReadBlock RAII. This is ML2 from the match-lift doc ("IN RESEARCH"). Fixing this compiler/scheduling issue could unlock ~600 functions.

2. **Template goldmines never attempted** — GetManagedType / GetDataDirectory / GetFileExtension / IsManagedTypeExternalStatic: 172 functions (43 each), zero ever tried. All 257-512B. Almost certainly identical template instantiations — cracking 1 exemplar of each unlocks all 43.

3. **The >512B cliff is unresearched** — only 21 ever attempted across all >512B functions, 2 matched. 1,691 untried holding 2.17MB. We don't know WHY they fail. Could be VFPU, structural complexity, missing headers, or the same prologue issue.

High-yield patterns with proven hit rates still exist:
- GetText: 58 matched / 2 failed / 154 untried (~97%)
- Evaluate: 25 matched / 4 failed / 161 untried (~86%)
- 257-512B misc (excl Read/AssignCopy): ~900 untried at ~78% expected rate

## Plan

### Phase 1: Template crack + cReadBlock research (interactive, this session)
- Match 1 exemplar each of the 4 template families (GetManagedType, GetDataDirectory, GetFileExtension, IsManagedTypeExternalStatic). Once cracked, orchestrator template machinery handles the other 168.
- Deep-dive the cReadBlock prologue divergence. Compare matched vs failed Read() disassembly, isolate the exact divergence, determine if it needs a compiler patch or source workaround.

### Phase 2: Overnight run — harvest known-good patterns
- Target: GetText (154), Evaluate (161), 257-512B misc non-Read (~900)
- Expected: ~800 matches, ~300KB gained
- Model: Sonnet for GetText/Evaluate (template-like, strong exemplars). Opus for varied 257-512B.

### Phase 3: Read campaign (depends on Phase 1 research)
- If prologue fix found → Read-focused run, expect massive yield
- If no fix → smaller retry batch with improved exemplars to measure marginal improvement

### Phase 4: >512B frontier research
- Pick 10-20 representative 513-1024B functions from diverse classes
- Interactive opus deep-dives to understand failure modes
- Determines whether the >512B cliff is fixable with current tools or needs Phase 4 "hard functions" tooling

## Log

_Updated as work progresses._

### Merged overnight run
- Verified `overnight/20260430-231647`: 49 matches, 51 failures, 0 errors, no unmatchable classifications, no tooling modifications. Clean ff-merge into main, branch deleted.

### CLAUDE.md audit + session journal system
- Created `docs/sessions/` with README and this file
- Condensed CLAUDE.md: cut redundant elaboration, added doc map table, added "read latest session" instruction
- Committed: `e0e86bc`

### Phase 1 research findings
- **cReadBlock prologue divergence: confirmed unfixable at source level.** ML2 in enhancements-match-lift.md documents exhaustively — 48 confirmed identical 8-instruction prologue reorderings, all source workarounds exhausted (9,600+ permuter candidates, inline asm, pragma, register binding). Fix requires binary patching `pspcor.exe` CG_sched/CG_LRA. Deferring to dedicated research session.
- **Template methods: immediately actionable.** 172 functions across 43 `*Group` classes (eTextureGroup, eMaterialGroup, gcEntityGroup, etc.), all 292-316B, all with existing source files and matched sibling functions (Write, New, AssignCopy, GetType). Tight size distribution confirms template instantiations. Attempting to crack one exemplar of each.

### Template method matching
- Cracked all 4 template families in eTextureGroup: GetManagedType (292B), GetDataDirectory (296B), GetFileExtension (296B), IsManagedTypeExternalStatic (312B). All first-try matches except IsManagedTypeExternalStatic which needed explicit bool conversion pattern (`li v0,0; bnezl; li v0,1` instead of `andi v0,v0,1`).
- Pattern: 4-level nested `cType::InitializeType` lazy-init. Levels 1-3 use shared globals (D_000385DC/E0/E4). Level 2 has `cNamed::New` as factory. Level 4 is per-class (different global, string addrs, count). All 43 classes have identical sizes per method → confirmed template instantiation.
- 168 remaining instances (42 classes × 4 methods) now unblocked for overnight agents.
- Committed: `8b24974`

### GetDesiredType exemplar
- Matched gcDesiredEntityTemplate::GetDesiredType (296B). Same 4-level lazy-init as *Group templates but with gcEntityTemplate::New as level-4 factory and type_kind=0x8E. 14 remaining 296B instances unblocked.
- compare_func.py had a symbol lookup collision (mapped GetDesiredType mangled symbol to Get(bool) DB entry). Manual byte comparison confirmed match. May need a DB/tool fix for overnight agents to pick these up cleanly.
- Committed: `b760e69`

### cReadBlock research conclusion
- Confirmed ML2 (prologue scheduler divergence) is the sole blocker for ~600 Read() functions. Not solvable at source level. Requires binary patching of pspcor.exe CG_sched/CG_LRA — a dedicated research session.

## Outcomes

**Matches this session:** 6 (4 *Group template exemplars + 1 GetDesiredType + 1 IsManagedTypeExternalStatic bool fix)
**Bytes matched:** ~1,500
**Functions unblocked for overnight:** ~197 (168 *Group templates + 14 GetDesiredType + 15 misc same-pattern)

**Key findings:**
1. The 4 *Group template families (172 functions) are pure template instantiations — identical code, different string/global addresses behind relocations. One exemplar per family is sufficient.
2. cReadBlock prologue divergence confirmed as compiler-level (ML2). ~600 functions blocked until pspcor.exe is patched.
3. The >512B cliff remains unresearched — needs a dedicated deep-dive session.

**VisitReferences research (>512B breakthrough opportunity):**
- 305 untried VisitReferences >512B with ZERO matched exemplars at that size (all 25 existing matches are ≤156B)
- Attempted gcFunction::VisitReferences (516B). Got to 496/516B (20B short). Pattern identified:
  1. Call parent `cObject::VisitReferences` (passthrough)
  2. 3-level type init (D_000385DC/E0/E4, same as GetManagedType)
  3. Dynamic cast on sub-object member via VRDispatchEntry at typeInfo+8 (same dcast pattern as AssignCopy)
  4. Flag mask check: `(flags & 0xFE00) & *(unsigned short *)(casted + 0x28)`
  5. Conditional callback + delegation to `gcExpressionList::VisitReferences`
- Symbol name mismatch: SNC uses back-references (`TB`) in mangling that our compiler emits literally (`Ui`). Not a code issue — compare_func needs the DB mangled_symbol for lookup.
- 20B diff likely from flag check or branch structure. Good candidate for permuter or manual iteration by overnight agent.
- Cracking this pattern would unlock ~305 functions / ~200KB — the single biggest remaining category.

**Next: overnight run targeting:**
- GetText (154 untried, 97% hit rate)
- Evaluate (161 untried, 86% hit rate)  
- *Group templates (168 untried, expect ~95%+ given exemplars)
- GetDesiredType variants (14 untried)
- 257-512B misc non-Read (~900 untried, ~78% hit rate)
