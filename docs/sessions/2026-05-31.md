# Session 2026-05-31 — 10-hour byte-exact matching run

**Branch:** `overnight-2026-05-31` (from main `4695698`).
**Mandate (operator):** "10 hour workflow run just trying to match hard… similar
compute and strategy as [Session 1]… permission to complete research/tooling
projects along the way when needed… get me as many legit matches as possible."

## Strategy (same as Session 1)

One isolated parallel matching engine (`overnight_match_parallel.js`, ~10–14 agents
/round, hard-isolation: one new src file per agent), high-yield slice selection,
harvest → byte-exact re-verify + asm-content audit → `/pre-commit-review` → commit.
CPU permuters fed by `near_miss` outputs. Conductor backstop drives the loop;
wind-down + ff-merge to main at end of 10h.

## Round 1 (in flight)

14 independent functions, 212–232B band (the ≤256B small pool is exhausted).
Mix: list-unlink (eBroadphase::UnlinkPair), parsers (cParser::NextAny,
cParserW::ValueAny), GetText-family (gcDesiredUIWidgetHelper::GetPrimaryText),
libc (`__sflags`), and likely-hard VFPU (eFilteredTexture::GetFullTexCoords sibling,
eSolverRow::ApplyUnembedImpulse, eCollision::GetPointPointContacts). Engine
`w3tbrrbtc`.

## MAJOR FINDING — VisitReferences mangling divergence (ADR-014)

While assembling the CPU-permuter near-miss pool, two things surfaced:

1. **The documented "#1 leverage" (permuter GPR-recoloring) is ALREADY BUILT.**
   `tools/mutations_regalloc.py` already registers `duplicate_local`,
   `introduce_temporary`, `inline_temporary`, `sink_computation`,
   `hoist_computation`, `rename_local`. No tooling work needed there.

2. **All 346 `VisitReferences` methods are blocked by a single mangling-algorithm
   divergence — and their CODEGEN IS ALREADY BYTE-EXACT.** See `docs/decisions/014`.
   Original SNC uses a cumulative (outer-inclusive) backref table inside
   function-pointer params (inner `unsigned int` → `TB`); pspsnc 1.2.7503.0 uses a
   fn-ptr-local table (→ literal `Ui`). Proven byte-identical on cObject (120B) and
   cBase (60B). No source change can fix it (exhaustively tested).

### OPEN OPERATOR DECISION (teed up — NOT actioned autonomously)

Two routes to credit these 346 byte-exact functions:
- **(A) Patch the SNC mangler binary** — norms-blessed (CLAUDE.md: fix compiler
  divergences in the compiler). ~2–4 day RE effort (cf. ADR-011 bnel patch), high
  blast radius (regression-test all 5214 matches before deploy). ROI is now far
  better than bnel ever was: 346 functions vs 2–7. Plausibly the biggest single
  unlock in the project.
- **(B) Normalize the verifier symbol gate** (`find_db_func_for_sym` in
  `tools/byte_match.py`) to treat the literal-vs-backref encodings of the *same*
  signature as equivalent. Hours, low technical risk (byte compare still gates), but
  in tension with the explicit "no post-processing of compiler divergences" norm.

**Decision deferred to operator.** Not implemented autonomously because it touches
the canonical match criterion / compiler binary and the explicit CLAUDE.md norm
points at (A). Reclassification of the 346 to `unmatchable_symbol_mangling` is also
held until A/B lands (if either is taken, they become creditable). Conductor will
NOT feed VisitReferences functions to the matching engine (guaranteed no-credit
under current tooling).

## Round 2 (236B band) — 4 matches

cObject::ReadHeader, cFactory::LoadLocalized, gcUI::CloseAllDialogs,
gcMap::PlayCinematic — all byte-exact, all pure C++ (0 asm), 4-agent review PASS.

**HELD (asm-ban):** `eConvexHullUtil::Finish` (0x0006e40c, 236B) compiles byte-exact
BUT the final asm block hand-writes the epilogue (`jr $ra` + `sv.q C120, 0x20($a0)`
in the delay slot, reading `this` via hardcoded `$a0`, with a dead
`addu $zero,%3,$zero` to fake the constraint binding). Hand-written control flow +
hardcoded arg-reg = banned (same class as last session's GetInertialTensor reject).
Caught by the asm-content audit, not the self-reported ratio. Legit path: use a
compiler-generated v4sf struct store for the tail so SNC schedules `sv.q` into the
`jr` delay slot itself (the round-1 "different-path compiler v4sf" idiom). Left
untracked for a future legit attempt; address reserved in exclude list.

Other round-2 misses: cFactory::WriteGroups (9B REG_ALLOC s1<->s3, permuter
confirmed no gradient — ADR-012 wall), cGroup::CreateObject (8B block-layout
codegen divergence, not bnel — failed), cParser::ValueAny / FileExists / CastRay /
CalcCRC / gcMsgEntitySpawn::Read / DeleteString (structural, need more AI work).

**CPU permuter** on FindAvailableChannel (round-1 near-miss): 8584 candidates, 0
improvement (best 46i worse than 36b baseline) — confirms the residual is pure
context-dependent allocator drift (ADR-012), no source leverage. File untouched.

## Round 3 (236-240B band) — 7 matches (best round)

mRay::Intersect (VFPU vsub.t/vdot.t + mfv/mtc1 transfer glue, clobber-listed scratch;
permuter --score-mode insns cracked the last 6B reg residual in 36s), nwConnection::Close,
__sfp (newlib glue-chain), gcDoFunction::~gcDoFunction (.word 0x1000ffff trailing-trap,
234-file precedent), eCollision::InitContactFunctions (34-reloc fn-ptr table init),
eFont::ePrinter::PrintString, gcMap::LoadBackground. 4-agent review PASS.

Note: gcMap::LoadBackground emits a harmless extra LOCAL __sti__ static-init symbol
(static gcBackgroundLoader gLoader w/ non-trivial ctor); target fn is byte-exact, compare
ignores the local symbol — left as-is (editing risks the verified match).

Misses/near (permuter-fueled): eCollision::UpdateClosestLine (12B, __builtin_ftovs/vstof
SNC VFPU intrinsics — legit C intrinsics not asm), eSweptVolumeCollisionHandler::
ProcessSavedContacts (13B) — both structurally complete, CPU permuters running.
strncasecmp (134B whole-fn coloring), cStrCompare (137B), eMemCard::BeginSave (98B),
eNavMeshPos::Read (27B, uses hardcoded $4/$20 in asm — would need audit IF cracked),
cBaseArray::RemoveAll (emitted a garbage anchor symbol — broken attempt, excluded).

## Round 4 (240-244B band) — 6 matches

gcMap::MemCardReplicateStreamedCinematicLoad, gcDoSwitchCase::Evaluate,
nwConnection::Flush, __sccl (libc scanset scanner), vfprintf (newlib nano dispatcher),
nwConnection::nwConnection (244B ctor). All pure C++ (0 asm), 2-agent review PASS.

Misses/near: nwConnection::ResendReliable (61B REG_ALLOC, permuter 0 gradient — ADR-012),
vfwprintf (11B — wide sibling of matched vfprintf, near), wmemchr (55B), memcmp (193B),
_sbrk (136B, SDK syscalls), eConvexHullShape::GetProjectedMinMax (131B VFPU),
cDynamicMemAllocator::EndBlock (240/244B size-miss), eCamera::UpdateWorldToProj
(228/244B size-miss, VFPU matrix). CPU permuters on the round-3 12B/13B near-misses:
ProcessSavedContacts 0 improvement (ADR-012); UpdateClosestLine errored loud (needs
-Xvfpuscalar for its VS-mode builtins — permuter flag gap, near-miss stays open).

## Round 5 (244-252B band) — 7 matches

nwConnection::BufferInBlock (decl-ORDER lever fixed callee-save coloring: declaring
`end` before `cur` forced s5/s6 to match), eMoviePlatform::decode_audioDecode,
eSprite::GetTexCoord1, eSprite::GetTexCoord2, eSolverRow::WarmStart (VFPU vector-scale,
same sanctioned idiom as ApplyUnembedImpulse), gcNetGame::GetNumActiveConnections,
gcBipedController::OnActivated. 2-agent review PASS (eSolverRow VFPU deep-audited clean).

Misses/near: eCamera::GetOrthoRect (8B — SNC ties unary-minus to first-loaded reg,
no source lever), gcGameSettings::ProfileFind (13B), eBoxShape::GetProjectedMinMax (77B
REG_ALLOC + VFPU, ADR-012; permuter's m2c scorer can't see through inline-asm VFPU body),
gcEntity::StopSound (31B), gcCamera::gcXYZMotion::Read (49B), cStrCompare-char (149B),
gcLookAtController::LookAt (240/248 size-miss). The eCamera/ProfileFind small near-misses
are operand-canonicalization/coloring with no source lever.

## Round 6 (252-256B band) — 7 matches

cManager::Add (decl-order + int-cast forced addu operand order to crack last byte),
eDynamicGeom::UpdateLocalToWorld (46% VFPU, legit "=X" passthrough — auditor deep-checked
clean), gcUIWidgetGroup::InsertIntoDialog (keeps a permuter-named local v90882 — name-hash
load-bearing per the mBSpline finding; do NOT rename), mFrustum::GetCorners, nwConnection::
Receive, eMultiSphereShape::CastSphere, gcRegionSetGroup::ClearRegionSetState. 2-agent
review PASS.

**HELD (asm-ban reject):** eCamera::CalculateWorldToCamera (0x0002eac0, 256B) compiles
byte-exact BUT pins all 4 args to $a0-$a3 via `register T* r __asm__("$a0")=arg;` then
reads them through HARDCODED $a0 in the asm body + does integer-MIPS address math
(lui/addiu) in asm + wraps the whole function in one asm block (>50%). Reading args via
hardcoded arg-registers is banned (same class as eConvexHullUtil::Finish). Caught by the
asm-content audit. Legit path: v4sf native types + compiler-generated loads with args as
%N operand constraints (the eDynamicGeom "=X" pattern). Left untracked; address reserved.

Misses/near: mPlane::GetMirrorMatrix (12B), eDynamicModel::SnapControllerTo (57B REG_ALLOC,
3 permuter campaigns / 26k candidates plateaued — ADR-012), gcUIWidget/gcUIDialog::
ProcessesMessage (~155B), mFrustum::GetFOV (151B), cStrNCompare (157B).

## Round 7 (256-260B band) — 6 matches

_multadd, __kernel_sinf (fdlibm), gcDoMouseOp::Evaluate (added "load-bearing, do not
remove" note to its dead-write coloring scaffolding), eFont::ePrinter::CalcRectAddSprite,
eNavMesh::GetSolidVerts, eStaticSurfaceLight::GetDirectLight (LEGIT VFPU: ~34% asm, all
constraint-bound, __builtin_vstof/ftovs intrinsics — the correct pattern eCamera should
have used). 2-agent review PASS.

**DB DATA BUG found:** gcEntityTemplate::FindRelationshipSet — DB signature says 4th param
is `const cHandlePairT&` but the mangled symbol `...PiTDK` proves it's `int*` (TD backref to
param D = the 3rd int*). Real sig: `(cHandlePairT&, bool, int*, int*) const`. Sibling
gcEntity::FindRelationshipSet (0x114ee8) same. Operator: worth a DB fix (affects any future
attempt). Still a 78B near-miss either way (reg-pair swap + frame-size, ADR-012).

Misses/near: nwSocket::Connect (5B), gcUIWidgetList::HandleMouse (3B — SNC folds 0&0xFF to
li vs original andi, bnel-class compiler divergence, no source fix), gcMap::CreateRegion
(252/256 size-miss, one dead delay-slot move SNC's DCE removes), mBasis::SetH (11B),
eDynamicMesh::GetNodeIndex (89B). sceSasInitWithGrain unmatchable (SDK syscall stub,
obj_file=null). eMoviePlatform::read_thread 244/260 size-miss.

## Round 8 (260-264B band) — 5 matches

eMultiSphereShape::CastRay (sched=1 + init-order barrier), _fopen_r (newlib), cType::
InitializeType (264B, 11 relocs — type-registration), gcCinematicInstance::Play (removed
a dead duplicate struct flagged by reuse review), gcMap::ReadDynamicallyLoadedObjectsToLoad.
2-agent review PASS. Yield dipped (5) — the gc ProcessMessage / EntityControllerTemplate
template family (GetAnimation 41B, GetAttack 42B, ProcessMessage 216B) are reg-coloring /
structural misses.

Misses/near: eSimulatedController::ApplyPositionedImpulse (47B REG_ALLOC, 7616 permuter
candidates 0 progress — ADR-012), mTriangle::GetBarycentricCoordinate (86B), eWorld::
MoveIntoRooms (34B), several size-misses (Iterate, UpdateFollowFixedDirection,
ringbufferCallBack — 4-16B short, SNC delay-slot/DCE divergences).

## Round 9 (264-272B band) — 4 matches

gcBackgroundLoader::OnExit (.word trailing-trap), gcViewport::MemCardReplicate,
gcGame::FindStreamedCinematicAll, _wwrite. 2-agent review PASS. Yield down to 4 — the
264-272B band is dominated by gc-template (cHandlePairT) and FP/VFPU functions that land
as reg-coloring near-misses.

**Notable near-miss:** gcCameraModes::UpdateLookFromEntityNode — **1/268 bytes differ**
(single register/operand encoding). _reclaim_reent 10B. The gc*Template GetAnimation/
GetAttack family is a recurring exact-size REG_ALLOC near-miss (40-42B, ADR-012 — the
original monolithic TU coalesces the candidate pointer into a callee-saved reg; split-TU
can't reproduce). Permuter campaigns (4k+ candidates each) make zero progress on these.

**Permuter VFPU flag gap (noted):** the permuter's baseline compile uses only -Xsched=2,
omitting the per-file -Xvfpumatrix/-Xvfpuscalar, so VFPU near-misses (TestSeperationAxis,
UpdateClosestLine) bloat and self-skip. The permuter ACCEPTS --vfpumatrix/--vfpuscalar
args (documented) — agents just need to pass them for VFPU targets. Usage gap, not a bug.

## Running ledger

- main start: 5214 matched / 1151 failed / 2851 untried / 26 unmatchable (9242 total).
- after R1: 5219 matched (+5). after R2: 5223 matched (+4). Held: eConvexHullUtil::Finish.
